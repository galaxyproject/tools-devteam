<?xml version="1.0"?>
<tool id="ncbi_taxonomy_fetcher" name="NCBI" tool_type="manage_data" version="1.0.0">
    <description>taxonomy downloader</description>
    <requirements>
        <requirement type="package" version="7.35">libcurl</requirement>
    </requirements>
    <stdio>
        <exit_code description="Error" level="fatal" range="1:" />
    </stdio>
    <command>
    <![CDATA[
        curl -o taxdump.tar.gz ${taxonomy_url} && tar xzf taxdump.tar.gz && python $__tool_directory__/data_manager.py --out "${out_file}"
        #if $database_name:
            --name "${database_name}"
        #end if
    ]]>
    </command>
    <inputs>
        <param help="Enter a unique identifier, or leave blank for today's date" label="Name for this database" name="database_name" type="text" optional="True" />
        <param label="Enter URL for taxonomy files" name="taxonomy_url" type="text" value="ftp://ftp.ncbi.nih.gov/pub/taxonomy/taxdump.tar.gz" />
    </inputs>
    <outputs>
        <data format="data_manager_json" name="out_file" />
    </outputs>
</tool>
